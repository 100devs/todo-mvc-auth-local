<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>

<body>
    <h1>Todon'ts</h1>
    <ul>
        <% todos.forEach( el=> { %>
            <li class='todoItem' data-id='<%=el._id%>'>
                <span class='<%= el.completed === true ? ' completed' : 'not' %>'><%= el.todo %></span>

                <span><%= el.reasonWhy %></span>

                <span id="shame">
                    <%= el.timesDone %>
                </span>
                <div class="addShame">Click for Shame</div>
                <span class='del'> Delete </span>
            </li>
            <% }) %>
    </ul>

    <h2>
        <%= user.userName %> has <%= left %> bad habits that they are working on.
    </h2>

    <form action="/todos/createTodo" method='POST'>
        <input type="text" placeholder="Enter Todon't Item" name='todoItem'>
        <textarea placeholder="Enter Reason Why" name='reasonWhy'></textarea>
        <input type="submit">
    </form>

    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>

</html>